apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: overseerr-rewritebody
spec:
  plugin:
    rewritebody:
      rewrites:
        - regex: `((href|src)="(/))`
          replacement: `${1}overseerr`
