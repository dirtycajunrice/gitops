apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: traefik-forward-auth-do
  namespace: argocd
spec:
  destination:
    namespace: kube-system
    server: https://kubernetes.default.svc
  project: default
  source:
    path: traefik-forward-auth
    targetRevision: HEAD
    helm:
      valueFiles:
        - values.yaml
        - values-do.yaml
    repoURL: git@github.com:DirtyCajunRice/GitOps-Argo-CD.git
  syncPolicy:
    automated: {}
