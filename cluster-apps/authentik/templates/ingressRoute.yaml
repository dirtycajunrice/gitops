apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.domainName | replace "." "-" }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.domainName }}`) && PathPrefix(`/static`)
      #      middlewares:
      #        - name: someshit
      priority: 15
      services:
        - kind: Service
          name: authentik-static
          port: 80
    - kind: Rule
      match: Host(`{{ .Values.domainName }}`) && PathPrefix(`/if`)
      #      middlewares:
      #        - name: someshit
      priority: 15
      services:
        - kind: Service
          name: authentik-static
          port: 80
    - kind: Rule
      match: Host(`{{ .Values.domainName }}`) && PathPrefix(`/media`)
      #      middlewares:
      #        - name: someshit
      priority: 15
      services:
        - kind: Service
          name: authentik-static
          port: 80
    - kind: Rule
      match: Host(`{{ .Values.domainName }}`) && Path(`/favicon.ico`)
      #      middlewares:
      #        - name: someshit
      priority: 15
      services:
        - kind: Service
          name: authentik-static
          port: 80
    - kind: Rule
      match: Host(`{{ .Values.domainName }}`) && Path(`/robots.txt`)
      #      middlewares:
      #        - name: someshit
      priority: 15
      services:
        - kind: Service
          name: authentik-static
          port: 80
  tls:
    secretName: {{ .Values.domainName }}

