apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cluster-critical
spec:
  generators:
    - git:
        repoURL: https://github.com/DirtyCajunRice/GitOps-Argo-CD.git
        revision: refactor
        directories:
          - path: cluster-critical/baremetal
          - path: cluster-critical/cloud
          - path: cluster-critical/global/*
  template:
    metadata:
      name: '{{ path.basename }}'
    spec:
      project: cluster-critical
      source:
        path: '{{ path }}'
        targetRevision: refactor
        repoURL: https://github.com/DirtyCajunRice/GitOps-Argo-CD.git
        directory:
          recurse: false
      destination:
        name: do
        namespace: argocd
      syncPolicy:
        automated: {}
        syncOptions:
          - ApplyOutOfSyncOnly=true
