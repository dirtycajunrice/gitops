apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.priority: "15"
  name: {{ .Values.radarr.radarr.fullnameOverride }}-api
spec:
  rules:
    - host: nickflix.io
      http:
        paths:
          - path: /{{ .Values.radarr.radarr.fullnameOverride }}/api
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.radarr.radarr.fullnameOverride }}
                port:
                  number: 7878
  tls:
    - hosts: [nickflix.io]
      secretName: nickflix-io
