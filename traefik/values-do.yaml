global:
  cluster: do

traefik:
  deployment:
    replicas: 3
  additionalArguments:
    - "--providers.kubernetesingress.ingressendpoint.publishedservice=kube-system/traefik"
    - "--providers.kubernetesingress.ingressendpoint.hostname=traefik-do.k8s.cajun.pro"
    - "--pilot.token=cac885fb-0bee-4b6e-a316-ceaf61cab202"
  service:
    annotations:
      service.beta.kubernetes.io/do-loadbalancer-name: "traefik"

tfa:
  authHost: cajun.pro
  ingress:
    hosts:
      - host: cajun.pro
        paths:
          - /_oauth
    tls:
      - hosts:
          - cajun.pro
        secretName: cajun-pro
